# Folder: src
> L2 | 父级: /CLAUDE.md

> 游戏前端应用主目录，包含核心状态机、数学求解器、音频子系统与 UI 渲染模块

## 成员清单
- `main.ts`: 游戏前端程序总入口，负责模块组装、事件总线绑定与响应式同步
- `types.ts`: 全局实体定义与类型契约 (GridCoord, LevelData, UserProfile, CellState 等)

**⚠️ 自指声明**：一旦本文件夹新增/删除/修改文件或职责变动，请立即更新本文档。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
